(use-modules (skiim))

(define-datom <mono-datom>
  (sob-dat <string>))

(define-datom <duo-datom>
  (one-dat <string>)
  (two-dat <string>))

(define-datom <prikriom>
  (ful <string>)
  (ssh <string>)
  (keygrip <string>)
  (test <string>))
